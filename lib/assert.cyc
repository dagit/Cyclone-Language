#include "assert.h"
#include "core.h"
#include "stdio.h"

namespace Assert {
  using Stdio;
  `a AssertFail(string_t mesg) {
    fputs(mesg, stderr);
    fputs(" -- assertion failed\n", stderr);
    exit(-1);
    throw new Core::Unreachable("assert");
  }
}
