

#include <stdio.h>
#include <typerep.h>
#include "marshal.h"
#include <list.h>
#include "../src/absyn.h"

using List;
using Typerep;
using Marshal;

namespace Dog {
  
  struct Mutt {
    int x,y;
  };
  
  
  tunion Terrier {
    Yorkshire(int);
    Scottish(stringptr_t);
  };
  union Beagle {
    int snoopy;
    double dog;
  };
}

  tunion Typestruct mutt_rep = __gen(struct Dog::Mutt);
  tunion Typestruct terrier_rep = __gen(tunion Dog::Terrier);
  tunion Typestruct beagle_rep = __gen(union Dog::Beagle);

